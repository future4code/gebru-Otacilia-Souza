  // O tsconfig.json criado no comando tsc --init que é diferente do 
    //criado em sala no rootDir e no outDir por exemplo,porque,  por default,
    // os arquivos de input e os de output não são separados em uma pasta
    //src e build.